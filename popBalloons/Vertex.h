#ifndef VERTEX_H
#define VERTEX_H

// This is an example. Modify the fields to match what your shaders expect.
struct Vertex {
    float x, y; // Position
    float r, g, b, a; // Color
};

#endif // VERTEX_H